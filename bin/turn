#!/usr/bin/env ruby

require_relative '../lib/turn.rb'
board = [" "," "," "," "," "," "," "," "," "]
turn(board)
idex = input_to_index(number)
if valid_move?(board,index) == true
  move(board, idex)
  display_board(board)
else
  turn()
